import React from 'react';

const Header = () => (
  <header className="hero is-warning" style={{ padding: "2rem" }}>
    <h1 className="title has-text-centered">Handiwork</h1>
  </header>
);

export default Header;
